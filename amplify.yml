frontend:
    phases:
      preTest:
        commands:
          - npm install
      test:
        commands:
          - npm test && npm run build
    artifacts:
      baseDirectory: .next
      files:
        - "**/*"
    cache:
      paths:
        - node_modules/**/*
test:
  phases:
      preTest:
        commands:
          - npm install
      test:
        commands:
          - npm test
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*