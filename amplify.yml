frontend:
    phases:
      preTest:
        commands:
          - npm install
      test:
        commands:
          - npm test && npm run build
    artifacts:
      baseDirectory: build
      files:
        - "**/*"
    cache:
      paths:
        - node_modules/**/*